{
  "buildCommand": "yarn lint && yarn test && yarn build",
  "devCommand": "yarn dev",
  "installCommand": "yarn install",
  "framework": null,
  "outputDirectory": "dist",
  "github": {
    "silent": true
  }
}
